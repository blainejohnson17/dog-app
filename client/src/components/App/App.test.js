import React from 'react';
import { mount } from 'enzyme';
import App from './App.component';

// jest.mock('../../Services', () => ({
//   fetchFoo: () => Promise.resolve({}),
//   createFoo: () => Promise.resolve({}),
//   deleteFoo: () => Promise.resolve({}),
//   updateFoo: () => Promise.resolve({})
// }));

describe('<App />', () => {});
